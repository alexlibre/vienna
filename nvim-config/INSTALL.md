# Установка Neovim для Frontend разработки в Termux

Полная конфигурация Neovim для разработки на Vue.js и TailwindCSS в Android Termux.

## 🚀 Быстрая установка

### Шаг 1: Установка необходимых пакетов в Termux

```bash
# Обновление пакетов
pkg update && pkg upgrade -y

# Установка Neovim и зависимостей
pkg install -y neovim git nodejs python ripgrep fd lazygit wget curl unzip make gcc

# Проверка версии Node.js (должна быть >= 16)
node --version
```

### Шаг 2: Установка конфигурации Neovim

```bash
# Создание директории конфигурации
mkdir -p ~/.config/nvim

# Копирование конфигурационных файлов
# Предполагается, что вы находитесь в директории nvim-config
cp -r * ~/.config/nvim/

# Или клонирование из репозитория (если конфигурация в git)
# git clone <your-repo-url> ~/.config/nvim
```

### Шаг 3: Установка Node.js пакетов глобально

```bash
# Установка LSP серверов
npm install -g \
  @vue/language-server \
  vls \
  typescript \
  typescript-language-server \
  @tailwindcss/language-server \
  vscode-langservers-extracted \
  eslint

# Проверка установки
which volar-server
which typescript-language-server
```

### Шаг 4: Первый запуск Neovim

```bash
# Запуск Neovim
nvim
```

При первом запуске:
1. **lazy.nvim** автоматически установится
2. Все плагины начнут загружаться автоматически
3. Подождите завершения установки (может занять 2-5 минут)
4. После установки перезапустите Neovim: `:q` и снова `nvim`

### Шаг 5: Установка Language Servers через Mason

После первого запуска:

```vim
:Mason
```

В интерфейсе Mason:
- Нажмите `i` для установки
- Убедитесь что установлены:
  - `volar` (Vue)
  - `tsserver` (TypeScript)
  - `tailwindcss` (TailwindCSS)
  - `html` (HTML)
  - `cssls` (CSS)
  - `jsonls` (JSON)
  - `eslint` (ESLint)

## 📁 Структура конфигурации

```
~/.config/nvim/
├── init.lua                    # Главный файл конфигурации
├── lua/
│   ├── config/
│   │   ├── options.lua        # Базовые настройки Neovim
│   │   ├── keymaps.lua        # Горячие клавиши
│   │   └── lazy.lua           # Настройка менеджера плагинов
│   └── plugins/
│       ├── treesitter.lua     # Подсветка синтаксиса
│       ├── lsp.lua            # LSP конфигурация
│       ├── cmp.lua            # Автокомплит
│       ├── nvim-tree.lua      # Дерево файлов
│       ├── git.lua            # Git интеграция
│       ├── telescope.lua      # Поиск файлов
│       ├── ui.lua             # UI плагины
│       └── extras.lua         # Дополнительные плагины
└── INSTALL.md                  # Эта инструкция
```

## ⌨️ Основные горячие клавиши

### Общие
- `<Space>` - Leader key
- `<Space>w` - Сохранить файл
- `<Space>q` - Выйти
- `<Esc>` - Очистить подсветку поиска

### Навигация по окнам
- `Ctrl+h/j/k/l` - Переход между окнами
- `Ctrl+Arrow` - Изменение размера окон

### Файловый менеджер (NvimTree)
- `<Space>e` - Открыть/закрыть дерево файлов
- `<Space>o` - Фокус на дереве файлов
- `a` - Создать файл/папку
- `d` - Удалить файл
- `r` - Переименовать файл
- `x` - Вырезать файл
- `c` - Копировать файл
- `p` - Вставить файл

### Поиск (Telescope)
- `<Space>ff` - Найти файлы
- `<Space>fg` - Поиск текста (grep)
- `<Space>fb` - Список буферов
- `<Space>fr` - Недавние файлы
- `<Space>fc` - Git коммиты
- `<Space>fs` - Git статус

### Git
- `<Space>gg` - LazyGit (интерфейс Git)
- `<Space>gb` - Показать blame строки
- `<Space>gp` - Предпросмотр изменений
- `<Space>gr` - Отменить изменения
- `<Space>gs` - Добавить в stage
- `]c` - Следующее изменение
- `[c` - Предыдущее изменение

### LSP (автодополнение и навигация)
- `gd` - Перейти к определению
- `gD` - Перейти к объявлению
- `gi` - Перейти к реализации
- `gr` - Показать ссылки
- `K` - Показать документацию
- `<Space>rn` - Переименовать символ
- `<Space>ca` - Действия с кодом
- `<Space>fm` - Форматировать файл
- `[d` - Предыдущая ошибка
- `]d` - Следующая ошибка

### Автокомплит (в режиме вставки)
- `Ctrl+j/k` - Навигация по списку
- `Ctrl+Space` - Вызвать автокомплит
- `Enter` - Подтвердить выбор
- `Tab` - Следующий пункт / следующий сниппет
- `Shift+Tab` - Предыдущий пункт / предыдущий сниппет
- `Ctrl+e` - Закрыть автокомплит

### Терминал
- `<Space>t` - Открыть терминал
- `Esc` - Выход из режима терминала (в normal mode)
- `Ctrl+h/j/k/l` - Навигация между окнами из терминала

### Комментирование
- `gcc` - Закомментировать строку
- `gc` (visual mode) - Закомментировать выделение
- `gbc` - Блочный комментарий

### Буферы
- `Shift+l` - Следующий буфер
- `Shift+h` - Предыдущий буфер
- `<Space>bd` - Закрыть буфер

## 🎨 Возможности

### ✅ Подсветка синтаксиса
- **Treesitter** - современная подсветка синтаксиса для Vue, TypeScript, HTML, CSS и др.
- Автоматическая установка парсеров при открытии файлов

### ✅ Дерево проекта
- **NvimTree** - файловый менеджер с иконками
- Интеграция с Git (показывает статус файлов)
- Быстрая навигация по проекту

### ✅ Git интеграция
- **Gitsigns** - показывает изменения в коде
- **LazyGit** - полноценный терминальный интерфейс для Git
- Быстрый доступ к Git операциям

### ✅ Встроенный терминал
- Встроенный терминал Neovim
- Разделение окон для работы с кодом и терминалом одновременно

### ✅ Автокомплит
- **nvim-cmp** - умное автодополнение
- Интеграция с LSP серверами
- Сниппеты для быстрого написания кода
- Автокомплит путей, буферов и командной строки

### ✅ Поддержка Vue.js
- **Volar (Vue Language Server)** - полная поддержка Vue 3
- Автокомплит компонентов, props, emits
- Диагностика ошибок в реальном времени
- Навигация по компонентам

### ✅ Поддержка TailwindCSS
- **TailwindCSS Language Server**
- Автокомплит классов Tailwind
- Предпросмотр цветов
- Диагностика несуществующих классов

### ✅ Дополнительно
- Автозакрытие скобок и тегов
- Подсветка TODO комментариев
- Плавный скроллинг
- Красивая цветовая тема (Catppuccin)
- Информативный statusline (Lualine)
- Табы для буферов (Bufferline)
- Стартовый экран с быстрым доступом

## 🔧 Настройка для вашего проекта

### TypeScript SDK для Vue

Если в вашем проекте есть локальный TypeScript:

```lua
-- В ~/.config/nvim/lua/plugins/lsp.lua уже настроено:
init_options = {
  typescript = {
    tsdk = vim.fn.getcwd() .. "/node_modules/typescript/lib"
  }
}
```

### Tailwind конфигурация

Убедитесь что в вашем проекте есть `tailwind.config.js` или `tailwind.config.ts`.

### ESLint

Если используете ESLint, убедитесь что он установлен в проекте:

```bash
npm install -D eslint
```

## 🐛 Устранение проблем

### Проблема: LSP серверы не запускаются

```bash
# Проверьте установку Node.js
node --version  # Должна быть >= 16

# Переустановите LSP серверы
npm install -g @vue/language-server typescript-language-server

# В Neovim проверьте логи
:LspInfo
:Mason
```

### Проблема: Медленная работа

```lua
-- Отключите неиспользуемые плагины в lua/plugins/*.lua
-- Или увеличьте updatetime в lua/config/options.lua
opt.updatetime = 500  -- Вместо 300
```

### Проблема: Не работает автокомплит

```vim
" Проверьте статус cmp
:CmpStatus

" Убедитесь что LSP серверы запущены
:LspInfo
```

### Проблема: Treesitter ошибки

```vim
" Переустановите парсеры
:TSUpdate

" Или установите конкретный парсер
:TSInstall vue typescript javascript
```

### Проблема: LazyGit не найден

```bash
# Установите lazygit в Termux
pkg install lazygit

# Или используйте только Gitsigns (встроено)
```

## 📚 Полезные команды Neovim

```vim
:checkhealth           " Проверка здоровья конфигурации
:Lazy                  " Управление плагинами
:Mason                 " Управление LSP серверами
:LspInfo               " Информация о LSP
:TSUpdate              " Обновление Treesitter
:TSInstall <язык>      " Установка парсера
:NvimTreeToggle        " Открыть/закрыть дерево файлов
:Telescope             " Открыть меню Telescope
:LazyGit               " Открыть LazyGit
```

## 🎯 Следующие шаги

1. Изучите горячие клавиши - они значительно ускорят работу
2. Настройте под себя `lua/config/keymaps.lua`
3. Добавьте свои плагины в `lua/plugins/`
4. Изучите `:help` для интересующих функций
5. Попробуйте разные цветовые темы (в `lua/plugins/ui.lua`)

## 💡 Советы для Termux

- Используйте **внешнюю клавиатуру** для комфортной работы
- Установите **Termux:API** для дополнительных возможностей
- Настройте **долгое нажатие** клавиши громкости как Escape
- Используйте **split-screen** Android для просмотра документации

## 📖 Дополнительные ресурсы

- [Neovim документация](https://neovim.io/doc/)
- [Volar документация](https://github.com/vuejs/language-tools)
- [TailwindCSS IntelliSense](https://github.com/tailwindlabs/tailwindcss-intellisense)
- [Termux Wiki](https://wiki.termux.com/)

---

**Автор:** Конфигурация создана для Frontend разработки на Android планшете  
**Дата:** 2025-11-11  
**Версия:** 1.0

Приятной разработки! 🚀
